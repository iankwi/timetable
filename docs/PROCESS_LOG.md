# 开发流程日志（按时间记录，便于复现）

## 2026-08-29 第 1 轮：需求澄清与数据准备

1. 需求头脑风暴：教师多班级课表 + 日历待办 + 甘特长任务；GitHub 调研（WakeupSchedule_Kotlin、timetable、Fossify Calendar、smart_calendar 等），结论：无现成全占项目，Flutter 组件拼装自研
2. 用户提供课表图片（微信图片路径），多模态读取后**先出预览表请本人核对**再转格式 ← 该流程应固化为惯例
3. 用户核对后纠错：班级计数应为 四.1~四.7、六.1~六.7 各 7 节（每班每周 1 节）；学期 2026-09-01 ~ 2027-01-27
4. 生成 timetable.json / .csv / .ics（复现脚本要点：第5/6节下午用 14:00/14:55；ICS 事件从开学后该星期几首次出现起 weekly 重复）
5. 按 WakeUp 模板（用户提供的 xlsx，7 列）生成 timetable_wakeup.xlsx，周数填 1-22（8/31 那周为第 1 周），App 内开学日期须设 2026-09-01 才能对齐

## 第 2 轮：原型验证（HTML 单文件，替代 Flutter 快速验证）

- 电脑无 Flutter/Android SDK，改用单文件 HTML 原型 + 局域网 http.server(8080) + 手机浏览器访问（电脑 IP 192.168.110.151）
- 初版三视图：今天/周课表/月历

## 第 3 轮：用户反馈 → 修复记录

| 反馈 | 处理 |
|------|------|
| 周视图不适配安卓、要滑动看 | 改全宽自适应网格，禁止横向滚动（后在第 5 轮改为网格+面板） |
| 没有添加日程按钮 | 加浮动 + 按钮和日期详情添加入口；待办存 localStorage |
| 课表变动只能改源码？ | 加"导入课表"按钮，读 timetable.json 覆盖（兼容 start_date/end_date 字段） |
| 作息时间改了要改源码吗 | 不用——时间在 JSON 里；正式版应把"作息表"与"课表"拆成两个独立数据（开放问题 #3） |
| UI/UX 区别与优化时机 | UX 流程原型阶段定型；UI 精修正式版；"禁 emoji"作为硬约束即时生效 |
| 不喜欢 emoji | 全部移除，用文字/■□/＋/✕ |
| 要用 UX/UI 技能 | 安装 ui-ux-pro-max 到 ~/.agents/skills/；按其准则修复：删除确认、aria-label、触控目标、touch-action、对比度 |

## 第 4 轮：重大交互重构（用户澄清需求）

- 周视图改为"上网格 + 下方当天面板"：点列头切换日期，面板显示该日课程+待办+添加（参考月视图交互）
- 调课双轨制：长按有课格子(400ms, 震动反馈)拖到空格子 → 弹窗选【仅当天(临时)】/【整学期(长期)】；临时调课在格子标"调"
- 月视图加学期过滤：学期外日期置灰无课

## 第 5 轮：Bug 复现与修复

- **Bug：周视图列头显示周二~周六**。用户报告后用 browser-use（390×844 视口）复现成功
- 根因：JS 数组 0 起始，`WD[w]`（w 从 1 开始）取标签偏一天；`WD[d.getDay()]` 在周日的映射也是错的
- 修复：业务索引 WD（1=周一）与 JS 索引 WDS（0=周日）分离，标签统一 `WD[w-1]` / `WDS[getDay()]`
- 回归验证：列头周一~五 ✓；月视图 8 月全月学期外 ✓；9/1(周二) 仅第5节六.4 ✓；10/1(周四) 三节课 ✓
- 教训：**涉及"第 X 个"的语义（星期、节次），必须先写清楚索引基准再动手；改动后要在真实视口尺寸下渲染验证，不能只看代码**

## 复现环境

- 转格式：python3 + openpyxl（pip3 install openpyxl）
- 原型服务：`cd prototype && python3 -m http.server 8080`
- 手机访问：同局域网浏览器开 `http://<电脑IP>:8080/timetable_app.html`，或直接把 html 文件发手机用浏览器打开
- 验证工具：ZCode browser-use（IAB，setViewportSize 390×844 + domSnapshot）

## 2026-08-29 第 6 轮：周视图格子空白 + 拖拽/滑动失效（browser-use 全链路验证）

用户反馈：周视图看不到格子内容；浏览器拖动无效；左右滑动不行。

| 问题 | 根因 | 修复 |
|------|------|------|
| 格子有底色但无内容 | 格子内容走 lessonsOn()（含学期过滤），假期中全部为空 | 网格改为显示固定课表模板（baseAt），学期过滤只作用于"实际课程"视图（今天/面板/月历） |
| 拖动无效 | 连带问题：格子无内容时可拖元素存在但用户无从下手；另发现桌面鼠标需长按 400ms 才激活 | 桌面鼠标按住移动 8px 即激活拖拽；触屏仍为长按 400ms |
| 左右滑动不行 | 第 4 轮重构时误删了滑动手势 | 恢复：main 区横向滑动（|dx|>60 且 |dx|>1.5|dy|）切换 今天→周课表→月历，拖拽进行中屏蔽 |
| 临时调课"成功"但无效果 | 弹窗允许对学期外日期临时调课，而学期外无课，调整不可见 | 学期外日期禁用"仅当天"选项，弹窗说明原因，仅保留整学期调课 |
| 跨天调课顶掉目标日课程（重大模型缺陷） | ADJ 结构为 {date, from, to}，把"周一第3节→周五第4节"当成同日换节次，在周一这天同时删 from 和 to 节次 | 重构为 {fromDate, fromP, toDate, toP}：调出日删课（格子淡化+“调出”角标），调入日加课（格子显示调入班级+“调”角标），两端日期独立生效 |
| doMove 参数错位（回归） | 改调用格式后函数签名未同步，fromDate 存成 undefined | doMove 内按 type 分别解析参数 |

验证（browser-use，390×844）：六.5 周一第3节拖到周五第4节 → 周一格淡化带"调出"、周五格显示六.5带"调"；周一面板剩 四.4/四.7（四.4 不再被顶掉）✓；周五面板第4节出现"六.5（临时调课）" ✓。测试用学期起点模拟后已恢复 2026-09-01 并清空调课记录。

遗留观察：拖拽成功弹窗可能被键盘/滚动遮挡的极端情况未测；触屏真机长按手感待用户实测（开放问题 #1 不变）。

## 2026-08-29 第 7 轮：用户真机/浏览器实测反馈 4 项修复

| 反馈 | 根因 | 修复 |
|------|------|------|
| 取消调课后"课程悬浮"残留且切换视图不消失 | ghost 元素挂在 body 上，仅在正常 mouseup 路径清理；touchcancel/切视图/关弹窗路径不清理 | 新增 clearDragState()：setTab、touchcancel 统一清除 ghost 与 dragging/droptarget 状态 |
| 周课表要显示日期（滑动才有意义） | 列头只有星期文字 | 列头加日期小字（如 周一 8/24），列头可点选当天 |
| 手机长按触发选择文字 | 缺 user-select 禁用 | .cell/.grid/.hd/.slot 加 user-select:none + -webkit-touch-callout:none |
| 弹窗没有"临时调课"选项 | 第 6 轮把学期外日期的临时选项整个禁用了，而测试时整周都在开学前，永远看不到该选项 | 改为任何日期都允许临时调课（假期补课场景合理）：lessonsOn 的调出/调入对任意日期生效，基课表仍仅学期内展开；弹窗加说明文字区分两种调课 |

回归验证（browser-use 390×844）：列头显示"周一 8/24"等 ✓；拖拽弹窗含 取消/仅当天/整学期 三选项及说明 ✓；取消后 ghost/mask 均清除 ✓；拖拽中途切视图 ghost 清除 ✓。

工程教训：用脚本批量替换内联 JS 时，替换文本里多了一个反引号导致模板字符串提前闭合、整个 script 崩掉（页面白屏）——**补丁后必须 node --check 提取脚本做语法校验再部署**（已纳入流程）。另：browser-use 输入通道偶发停滞（cua 事件不到达页面），先 set(true) 恢复可见性/焦点再重试，属于测试环境问题而非页面 bug。

## 2026-08-29 第 8 轮：迁移 FullCalendar（停止重复造轮子）+ 假日支持

决策：手写网格+自制拖拽连续 4 轮出 bug，用户提出用现成项目。调研结论：
- FullCalendar 标准包（core/daygrid/timegrid/interaction，MIT）：原生触屏/鼠标拖拽调课、月/周视图、事件渲染——全盘接管视图层
- SortableJS（MIT）：通用拖拽，日历场景不如 FullCalendar，未采用

v2 原型 prototype/timetable_v2.html（新文件名同时绕开手机浏览器缓存旧版问题）：
- 周视图=FullCalendar timeGridWeek（时间轴列=天），月视图=dayGridMonth，均支持左右滑动/切换
- 拖拽课程块落点 → 弹窗 选 仅当天(临时)/整学期(长期)/取消；时间反查节次，非节次时间自动还原
- 假日模型：DATA.holidays=[{date,name}]，年初已知法定假日内置（中秋 9/25、国庆 10/1~7 工作日、元旦 1/1），当天课程自动隐藏、月/周视图渲染"xx节 · 休"，导入 JSON 可更新（支持 holidays 字段）
- 补课：临近通知后，把课拖到假日/任意日期即生成临时安排，弹窗自动提示"目标日是假日：本次为补课安排"
- FullCalendar v6 从 CDN 改为本地 lib/（jsdelivr 在校园网不稳，且离线可用）；v6 无独立 CSS，样式由 JS 注入

已验证（browser-use 390×844）：开学周 14 节课渲染 ✓；拖拽周一六.5→周四 P3 弹窗→仅当天 ✓（ADJ 正确、周一剩 4/5 节、周四补入）；假日事件 6 条渲染 ✓；假日课程抑制（中秋/国庆全 0 节）✓。

环境备注：网络切换（手机热点）后电脑 IP 变为 10.163.13.124，手机访问 http://10.163.13.124:8080/timetable_v2.html；http.server 重启后需在 prototype 目录执行。

开放问题更新：
- 8. 已解决：拖拽改用 FullCalendar 原生实现；假日处理模型落地（假日清单+补课拖拽）
- 9. 新增：官方调休补课日（周末上班）公布后更新 holidays/补课数据；v1 timetable_app.html 保留为备份，稳定后删除

## 2026-08-29 第 9 轮：v2 细节打磨（用户实测反馈）

| 反馈 | 处理 |
|------|------|
| 顶栏周/月切换与下方标签重复 | 移除 FullCalendar 工具栏的视图按钮，只留 前后翻页+今天；视图切换统一由导航标签控制 |
| 9/10 六.5 格子是黄色 | 这是"临时调课"事件的标记色；已改为绿底+橙左边条+标题带（调）字，不再整块黄色 |
| 月历只看到时间看不到班级、格子满满 | 月视图事件改为只显示班级号（隐藏时间行、限制每天最多 3 条、字号缩小） |

待办/课程"只能今天或周视图看"的澄清：月历点某天即看当天详情（点日期格），不需要单独页面；月历中课程以班级号短标签呈现。待办在月历以橙点标记。

已验证：工具栏只剩翻页+今天 ✓；调课事件显示"六.5（调）"绿底 ✓；月视图事件仅班级号、无时间行 ✓。

## 2026-08-30 第 10 轮：拖拽调课闭环修复 + 月视图时间显示

| 问题 | 根因 | 修复 |
|------|------|------|
| 月视图不显示时间/班级不全 | eventContent 只渲染班级号 | 改为 班级+开始时间（如"六.4 14:00"） |
| 拖拽保存的班级名为空（反复出现） | handleDrop 从标题文字正则解析，标题格式一改就失效 | 事件携带 extendedProps{cls,p,adj}，拖拽直接读属性，与显示文字彻底解耦（治本） |
| 调整过的课不能调回/状态错乱 | 历史脏记录（空 cls 的 ADJ）+ 互逆记录不抵消 | 已有互逆抵消逻辑；空记录用新数据自然消除；实测 调出→调回 后 ADJ 自动清零、课表还原 |
| 切换视图日期跳回今天（上轮遗留确认） | 重建日历实例 | 上轮已改为单实例 changeView，本轮实测 周↔月 双向日期保持 ✓ |

工作流新增：实现前先搜 GitHub 现成方案（已写入 REQUIREMENTS §8.5）。

环境备注：网络再次切换，电脑 IP 192.168.5.12，手机访问 http://192.168.5.12:8080/timetable_v2.html。IP 随网络变是常态，正式打包 APK 后此问题消失。

决策记录：路线确认为"FullCalendar 原型收尾 → Capacitor 打包 APK"，Fossify 作为过渡查看工具，放弃 Flutter 重写与 WakeUp/vivo 套件路线。

## 2026-08-30 第 11 轮：周视图改回"节次行网格"（参考现成课表项目）

用户指出 v2 周视图 UI 异常（FullCalendar 连续时间轴 + 5分钟细格导致格子被拉长、大片空白），并要求参考现成项目。

调研结论：WakeUp/SuperCourse 等课表项目的周视图均为**离散节次行网格**（行=第几节+时间，列=星期，格高固定），连续时间轴是日历产品的范式，不适合课表场景。

重构：
- 周视图改为自定义节次行网格（FullCalendar 仅保留月视图），支持 ‹› 翻周（临时调课按周可见）
- 格子状态：正常课 / （调）调入 / 调出（淡化）
- 调课交互双模式：①点选（点课块→点空格→弹窗选 仅当天/整学期）②按住拖动（手指按住课块移动，目标格虚线高亮，松手弹窗）；假期/学期外照常可操作
- 已验证（browser-use）：列头"周一 8/31"格式 ✓；假期周空格子 ✓；开学周点选调课闭环（四.6 周三P3→周五P4，ADJ 正确、两日课程同步变化）✓
- 修复：start() 只跟踪有课格子导致点空格子无响应；列头"周周一"重复字；三容器常驻避免视图互删

遗留：拖动手势在 browser-use 里因输入通道延迟未自动化验证，需用户手机实测；拖动与页面滚动的冲突已用 touch-action:none + 12px 位移阈值规避。

## 2026-08-30 第 12 轮：调课模型重构为"日期覆盖表"（修复重复课程/无法撤销）

用户实测发现：多次调课后出现重复课程、无法撤销。根因：ADJ 移动记录列表在多次叠加时边界情况算不清，且周视图重构后撤销入口丢失。

**模型重构**（治本）：
- 废弃 ADJ 移动记录列表，改为 OVR 日期覆盖表：{ "2026-09-02": [{p,cls,adj} 全天课程列表] }
- 调课 = 源日期列表移除一项 + 目标日期列表添加一项（添加前清同格），每格天然唯一，结构上不可能重复
- 覆盖列表与基课表一致时自动删除（存储不膨胀）
- 撤销：HIST 快照栈（保留20步），周导航栏新增"撤销"按钮，逐步回退
- 月视图关闭拖拽（调整统一在周视图），删除 FullCalendar eventDrop/eventClick 的 ADJ 遗留
- 空格子不可作为调课源（防 undefined 班级）

**验证**（browser-use，数据层直接调用 applyMove）：
- 三重叠加调课（四.6→周五P4、六.3→周一P4、六.1→周三P3）：五个工作日无重复、无丢失 ✓
- 连续三次撤销：OVR 清空、五日课程完全还原基表 ✓
- GUI 单次流程（点选→弹窗→仅当天→撤销按钮）：通过 ✓

经验：多步 GUI 自动化受输入通道延迟/重复影响不可靠，数据层用直接函数调用验证，GUI 只做单流程冒烟；真机手感仍需用户实测。

## 2026-08-30 第 13 轮：节次补全 1~7 节 + 旧数据迁移

用户需求：周视图显示第1~7节全部行（第7节 15:40-17:10，用于灵活安排比赛集训）。
顺带修复：用户手机月视图空白——根因是旧存储数据缺新节次的时间定义，渲染课程事件时中断。

- DEFAULT 节次表补全：第1节 08:20-09:00、第2节 09:30-10:10、第7节 15:40-17:10（用户指定集训时段）
- 数据迁移：加载时自动合并缺失节次键（老数据只有第3~6节也能升级），缺 holidays 字段同样补默认
- buildEvents 防御：缺时间定义的节次跳过，不中断整月渲染
- 验证：老数据（仅3~6节）加载后周视图显示 7 行节次 ✓，月视图事件正常（70 条）✓

说明：第7节 17:10 结束超出原 slotMaxTime 17:00，因周视图已是自定义网格不受影响；月视图为 dayGrid 无时间轴，也不受影响。

## 2026-08-30 第 14 轮：月视图塌陷修复

用户报告：周视图翻月后切到月视图，日历塌成一条窄列。
根因：FullCalendar 在容器 display:none 时渲染/切视图，宽度按 0 计算。先显示容器再 changeView，渲染后补一次 updateSize()。
验证：周→翻周→月 切换后网格宽度 364/390 正常，日期与所在周一致 ✓。

教训（通用）：隐藏容器内的组件初始化必须在显示之后，或显示后强制重算布局。

## 2026-08-30 第 15 轮：月视图改 Timeblocks 式宽松布局

用户提供了参考 APK（/Users/kw/Downloads/vivo办公套件/base.apk），解包识别为 Timeblocks 本尊（com.day2life.timeblocks）——即用户最初的需求参照物。无需模拟器，直接从截图提取布局特征复刻：
- 日历高度 100% 撑满视口，6 行月格不滚动（此前 auto 高度导致拥挤）
- 日期数字加大加粗（14px）、今日格浅绿底
- 事件为小色条：班级+开始时间（用户此前明确要求班级时间都要显示），每天最多 3 条
- 表头/格线弱化，大量留白
验证（390×844）：格子高 119px、网格 699×364 铺满、事件正常 ✓。

经验：解包 APK（unzip + strings dex/manifest）可识别应用身份与功能，无需模拟器。

## 2026-08-30 第 16 轮：月视图极简化（对齐 Timeblocks）

用户反馈日期信息太具体。对齐 Timeblocks 月视图本质：**格子里只有日期数字**，不放任何课程文字。
- 月视图事件改为小圆点：绿点=正常课、橙点=临时调整、红点=法定假日（悬停/点击看详情由日点击面板承担）
- 移除事件文字与时间显示，无文字残留（验证：70 点、6 红点、无文字）✓
- 修复教训：补丁替换时旧 eventContent（标题正则版）未匹配成功导致两版并存，正则/字符串替换后必须 grep 确认无旧代码残留

## 2026-08-30 第 17 轮：月视图交互对齐 Timeblocks 日程表模块

APK 深度静态分析（strings/classes 全量）确认其日程表模块实体：日程 schedule、任务 todo（todo_this_day 添加到当日）、备忘 memo（带提醒）、区间 interval（跨日）、习惯 habit、贴纸/日期背景色。点日期格子弹添加菜单即对应这些实体。

本版实现（教师场景裁剪）：
- 月视图点任意日期格 → 当日面板（课程列表/区间/待办）+ 添加菜单三入口：日程（当日文字事项）、任务（可勾选待办）、区间（跨日任务，起止日期）
- 区间渲染为日期背景色（覆盖的格子浅橙底+琥珀色日期号），今天视图显示"进行中的区间"可删除
- 已验证：点 9/2 弹当日面板（4节课列表）✓；添加"比赛集训" 9/14-9/19 → 6 格背景色覆盖 ✓

未引入：习惯打卡、贴纸、备忘提醒（与教师场景弱相关，需要时再加）；今日视图的区间删除按钮暂用通用确认。

## 2026-08-30 第 18 轮：月历减法 + 条目编辑/删除

| 反馈 | 处理 |
|------|------|
| "1日""周一"等信息臃肿重复 | dayCellContent 只输出数字（1、2、3...），dayHeaderFormat narrow 只显示单字（一/二/.../日），表头去边框淡色 |
| 区间/任务添加后无法删除编辑 | 当日面板每条目右侧 ✕ 删除；点条目文字进入预填编辑表单（日期/内容可改）；面板内增删改后自动刷新不跳页 |

验证：表头"一,二,三,四,五,六,日"、日期纯数字 ✓；9/16 当日面板含课程+区间条目带 ✕ ✓；点"比赛集训"文字弹出预填编辑表单 ✓。

## 2026-08-30 第 19 轮：区间/任务内容直接可见

用户反馈任务与区间只有色块看不到内容。处理：
- 区间渲染为**跨日连续色条**，标题文字直接显示在条上（一条覆盖全区间，宽 304px 实测）
- 任务/日程渲染为当日小字条（任务蓝底、日程绿底，日程带【日程】前缀）
- 课程仍为圆点（保持月历概览属性），每天最多 5 条，超出折叠
- 分工：色条=内容可见，圆点=有事标记，点击日期=详情面板

## 2026-08-30 第 20 轮：月视图文字条被折叠修复

用户浏览器中仍只见圆点：根因是 dayMaxEvents=5 时事件按默认排序展示，课程圆点排在文字条前面，拥挤日期（3~4节课+区间+任务超5条）下文字条被折叠进"+更多"。
修复：事件加 order 优先级——区间/任务/日程 order=1（文字条永远优先显示），课程圆点 order=9（拥挤时先折叠圆点）；dayMaxEvents 放宽到 7（格子高119px 足够容纳）。
验证：9/14（3课点+区间+日程+任务=6条）全部显示，文字条在前，无折叠 ✓。

## 2026-08-30 第 21 轮：月视图文字条"看不见"修复

用户报告文字条只有色块没有字。根因：FullCalendar 主题默认给事件内容设置白色文字（rgb 255,255,255），我们的浅色底+白字=不可见；且仅设置在 .fc-event 上的颜色无法覆盖到内容 span 的继承链。
修复：对 rgev/tdev/mgev 在 .fc-event、.fc-event-main、span 三层用 !important 显式指定颜色。
验证：rgev 文字颜色 rgb(240,140,0) on rgb(253,240,227) ✓。
教训：FC 事件自定义渲染必须同时覆盖元素及其内容容器的 color（FC 默认白字）。

## 2026-08-30 第 22 轮：ui-ux-pro-max 全视图审查

方法：从准则库提取 触控目标/对比度/字号/aria 相关规则，浏览器内程序化体检（遍历元素计算尺寸与 WCAG 对比度），三个视图分别扫描。

发现与修复：
| 视图 | 问题 | 修复 |
|------|------|------|
| 今天 | 待办勾选框 18px 偏小 | 22px |
| 今天 | "暂无待办"等空态文字对比度 2.6 | #999→#66707a（≥4.5） |
| 今天 | 删除 ✕ 对比度 2.8 | 同上 |
| 周视图 | 操作提示文字对比度 2.6 | 同上 |
| 月视图 | 事件圆点 7px 偏小难辨 | 9px |
| 全局 | 导入课表按钮高 29px | padding 增至高 33px |

复查：三视图全部通过。误报说明：白字"导入课表"的对比度按其半透明背景计算为 1.0，实际背景是绿色头部，达标。

## 2026-08-30 第 23 轮：月视图信息层级再平衡 + 整体 UX 审查

用户指出：前一轮极简化矫枉过正，月视图只剩色块圆点，无法直接阅读整月安排。ui-ux-pro-max 信息层级准则支持：月历的职责是"不点击即可读出整月概览"。

再平衡（三层均可见内容）：
- 课程：班级小字条（如"四.6"），调整过的橙字带（调）——去掉的只是时间（用户嫌"太具体"的正是时间）
- 区间：跨日色条+名称（保持）
- 任务/日程：蓝/绿字条（保持）
- 每天最多 3 条防拥挤，点击日期看全部细节（含时间）

整体 UX 审查另发现并修复：右下角 ＋ 按钮在任何页都只加待办，与月历三类入口不一致 → 统一为打开"今天"的添加面板（日程/任务/区间）。

验证：9月渲染 66 个班级字条、9/2 显示四.6/六.3/六.2/六.6 ✓；圆点机制弃用（mdot 样式保留但不再渲染）。
信息层级总结：日期数字（主）→ 班级/区间/任务字条（概览可读）→ 日期面板（细节含时间）。

## 2026-08-30 第 24 轮：整体换肤"奶油暖阳·Timeblocks 风"（用户四选一选定）

方案来源：ui-ux-pro-max 配色库（Habit Tracker/Diary 类暖色系 + Soft UI 风格）。
- 色板：主色琥珀 #d97706、背景乳白 #fdf8ec/#fffbem 系、卡片 #fffdf6、点缀绿 #059669（未启用）；调出/调整标记改玫瑰色 #e0556b/#c2394f 与主色区分
- Soft UI 细节：卡片圆角 14~16px、柔和暖色投影（rgba(190,140,50,.08~.10)）、按钮圆角放大
- 对比度复查发现并修复：琥珀主色直接作字条文字对比度仅 2.8 → 字条文字统一深棕 #8a5a10（对比达标），日程字条同步
- 验证：月视图/周视图所有字条文字对比度达标 ✓

换肤实现说明：原代码大量使用 CSS 变量 --green，换肤时变量语义保留（--green 现指向琥珀主色），历史色值引用自动生效。

## 2026-08-30 第 25 轮：安装 OpenDesign 技能库并按 redesign-skill 方法重设计

用户不满意琥珀换肤。安装 nexu-io/open-design 的 4 个技能：redesign-skill（重设计方法论）、impeccable-design-polish、design-review、theme-factory（~/.agents/skills/）。

按 redesign-skill 流程（扫描→诊断→修复）诊断出的问题：
1. 琥珀色泛滥无主次（顶栏/课程条/区间/FAB 全是琥珀）——准则：强调色唯一，其余去饱和
2. 纯黑阴影不协调——准则：阴影应染背景色相
3. 无按压态/过渡——显得生硬
4. 重色顶栏压住界面——不够安静

重设计方向"暖纸 + 青绿墨"：背景暖纸白 #faf9f5，顶栏去色块改浅底墨字，唯一强调色深青绿 #0f766e，课程/字条青绿系，阴影染暖 rgba(87,64,28,.06~.07)，全站按钮补 transition+按压缩放，调课标记保留玫瑰色 #bb4a3c 作状态色。
验证：顶栏浅底墨字、全要素对比度达标 ✓。
技能安装记录：~/.agents/skills/{redesign-skill,impeccable-design-polish,design-review,theme-factory}。

## 2026-08-30 第 26 轮：Capacitor 打包 APK 成功

环境搭建（用户网络为热点，多处需绕行）：
- JDK：brew cask 需 sudo 失败 → Adoptium tarball 免安装（~/jdk17、~/jdk21，Capacitor 8 需 21）
- Android SDK：cmdline-tools 官方下载正常，sdkmanager 装 platform-tools/platforms;android-34/build-tools;34.0.0
- Gradle：wrapper 下载 services.gradle.org 被 JDK 证书链拒绝（热点网络对该域名干扰）→ curl 预下载 gradle-8.14.3-all.zip 注入 ~/.gradle/wrapper/dists/gradle-8.14.3-all/10utluxaxniiv4wxiphsi49nj/（hash 目录以 wrapper 实际创建的为准）
- Maven 依赖：android/build.gradle 加阿里云镜像（google/central 前置）

产物：我的课表-v1.0.apk（debug 签名，4.0MB，appId com.zhengwenjuan.timetable，应用名"我的课表"）
安装方式：微信文件传输助手发 APK 到手机 → 点击安装 → 允许未知来源。

数据迁移注意：APK 内应用存储与浏览器 localStorage 不互通，首次使用需在 App 内重新"导入课表"（timetable.json）并重建待办/区间。

## 2026-08-30 第 27 轮：导入选项 + 系统设置 + APK v1.1

- 导入课表改为弹层流程，新增"同时清空个人记录"勾选（勾选后导入会清空待办/日程/区间；课表本身始终是替换式覆盖）
- 头部新增"设置"：显示当前课表/学期信息；清空个人记录（课表保留）；恢复默认课表并清空全部数据（带确认）
- 验证：设置面板 ✓ 导入面板含勾选框 ✓ 勾选清空导入后个人数据归零、新课表生效 ✓
- 重新打包：我的课表-v1.1.apk（webDir 同步 cap copy → gradle assembleDebug）
- 流程修正：改原型后需 cp 到 www/ 再 npx cap copy 才会进 APK；此后每次发版按此三步

## 2026-08-30 第 28 轮：隐私修复 + 手机手势化（v1.2）

用户指出：设置里的"恢复默认课表"会把 APK 内置的郑文娟课表释放给其他装机者（隐私泄露）；交互偏 PC 按钮化。

隐私修复（结构性）：
- APK 内**不再内置任何个人课表**：DEFAULT_DATA 清空 lessons/teacher/subject，仅保留通用节次与假日
- 新用户首启：标题"我的课表（未导入）"+ 自动弹出导入引导
- 设置面板移除显示教师信息的"恢复默认"，改为"清除课表数据（回到待导入状态，个人记录保留）"与"重新导入课表"
- 发给同事的 APK 从此可以放心传播，不含任何人的课表

手机手势（v1 之后丢失的滑动全面回归）：
- 今天视图：左右滑切换 今天/周课表/月历
- 周视图：横向滑 = 翻上一周/下一周（起手在有课格子上的滑动仍属调课手势，不冲突）
- 月视图：横向滑 = 翻上/下个月（起手在事件上不触发）
- 触摸与鼠标双通道绑定（测试通道与真机都可用）

验证：新用户首启引导 ✓ 空白标题 ✓ 月视图滑动翻月 ✓ 周视图滑动翻周 ✓（方向：右滑=上月/上周）。
产物：我的课表-v1.2.apk。发给同事改用此版本。

## 2026-08-30 第 29 轮：系统设置重构（信息可编辑 + 数据管理语义分明）

用户指出：三按钮职责不清、缺少学年起止等基本信息编辑、"清空个人记录"看起来没效果（因为课程格子属于课表数据本就不在清空范围）、v1.1 之前的"恢复默认"泄露内置课表。

重构后的设置面板（两组分区、按钮上直接写影响范围）：
① 课表基本信息：教师姓名 / 科目 / 学年起止日期，可编辑保存（同步标题栏）
② 数据管理：
  - 清空个人记录：只清 待办/日程/区间/调课记录，课程表保留（按钮下注明）
  - 清除课表数据：只清课程表（回到待导入状态），个人记录保留，可随后导入新学期课表
隐私：APK 不内置任何教师课表（第28轮已做），设置面板只显示当前用户自己的信息。

验证：设置表单预填 ✓ 编辑教师/学期保存后标题同步 ✓ 按钮齐全。
产物：我的课表-v1.3.apk。用户手机上的旧版"清空个人记录看似无效"是预期语义（课程不属个人记录），v1.3 按钮文字已写明范围。

## 2026-08-30 第 30 轮：桌面小组件（今日课程）v1.4

实现（R10 部分落地）：
- 原生：TodayWidgetProvider（AppWidgetProvider），读 CapacitorStorage 中 widget_week JSON，渲染 今日课程列表（时间+班级）+ 教师名；30 分钟系统级自动刷新（跨天自动更新）；res/layout/widget_today.xml + drawable/widget_bg.xml（圆角白底描边）+ res/xml/widget_today_info.xml；Manifest 注册 receiver
- JS：writeWidget() 计算未来7天课程（含临时调课/假日）写入 Capacitor Preferences（key widget_week）；saveAll/启动后/每30分钟触发
- 网页版无 Capacitor 环境自动跳过（window.Capacitor 守卫）
- 产物：我的课表-v1.4.apk（4.3MB，含 @capacitor/preferences 8.0.1）

用户使用：桌面长按 → 添加小组件 → 我的课表 → 今日课程；打开 App 导入课表后小组件自动有数据。
未验证项：小组件实际渲染需真机添加后确认（无模拟器）。

## 2026-08-30 第 31 轮：双桌面小组件 v1.5

用户确认新版"今日课程"设计，并要求同时保留方案C（日期双栏）作为第二种组件。
- 组件一（今日课程）：标题含 总节数/剩余统计、教师+日期、课程（时间+班级+进行中标记）、当日任务（☐/☑）与日程；点击跳App
- 组件二（日期双栏）：左侧大号日期数字+星期，右侧课程列表最多3条（含←进行中）；点击跳App
- 两者共用同一数据源（widget_week），点击均通过 PendingIntent 打开 MainActivity
- DateWidgetProvider + widget_date.xml + widget_date_info.xml + Manifest 注册
- 产物：我的课表-v1.5.apk（含 @capacitor/preferences，双组件）

## 2026-08-30 第 32 轮：小组件预览 + 实时刷新 v1.6

用户反馈：①添加组件时盲选看不到内容预览 ②App 内改任务/调课后桌面组件不实时更新（30分钟才刷）。

修复：
- 预览：新增 widget_today_init/widget_date_init 示例布局（预置"共4节/进行中/任务"等样例文字），initialLayout 与 previewLayout(Android 12+) 均指向示例布局，选择器里所见即所得
- 实时刷新：新增本地插件 WidgetSyncPlugin（@CapacitorPlugin name=WidgetSync，refresh() 重绘两种组件），MainActivity onCreate 注册；JS writeWidget 写完数据后调用 WidgetSync.refresh()——调课/勾任务/导入后桌面立即更新
- 构建注意：@CapacitorPlugin 注解在 com.getcapacitor.annotation 包（无 com.getcapacitor.CapacitorPlugin 类）；本地插件用 BridgeActivity.registerPlugin(Class) 注册
产物：我的课表-v1.6.apk

## 2026-08-30 第 33 轮：上课提醒通知 v1.7

用户确认设计：每天只提醒当天第一节课，通知栏形式。
- 插件：@capacitor/local-notifications（底层 AlarmManager，声明 SCHEDULE_EXACT_ALARM/POST_NOTIFICATIONS）
- 设置面板新增 ③ 上课提醒：开关 + 提前量（5/10/15/20/30分钟）+ 保存（首次开启请求通知权限）
- scheduleReminders：取消旧排程 → 未来7天每天取最早节次，排"第X节 HH:MM 班级，N分钟后上课"通知；时间已过则跳过；数据变化后重排（调课/假日自动跟随）；App 启动 2 秒后重排一次
- 通知点开 → 打开App（插件默认行为）
- 产物：我的课表-v1.7.apk
待真机验证：vivo 系统的通知授权弹窗与通知到达（部分国产ROM需允许自启动/后台弹出）。

## 2026-08-30 第 34 轮：组件实时性修复 + 滑动动画 v1.8

用户反馈：①组件更新有延迟非实时 ②左右滑动无动画生硬。

修复：
① 竞态根因：writeWidget 中 Preferences.set 是异步，WidgetSync.refresh 在写入落盘前就执行，读到旧数据只能等30分钟兜底。改为 async/await——等待写入完成后再 refresh，组件与App状态真正实时一致。
② 滑动动画：参考 iOS 分页交互的简化标准——切页/翻周/翻月后内容按滑动方向 18px 滑入（200ms cubic-bezier(.2,.7,.3,1)+淡入）。手指跟随的完整分页需原生重写视图层，收益比低（记录为放弃项）。
产物：我的课表-v1.8.apk。

## 2026-08-30 第 35 轮：周课表恢复 1~7 节 + 周六日列（v1.9）

用户反馈：周课表丢失第1/2/7节、不显示周六日、假期周无法拖动调课。

诊断与修复：
- 根因1（节次行）：迁移代码会补全 periods 1/2/7，但周视图此前在部分状态下仅渲染旧数据里的节次——统一按 DATA.periods 全量渲染（1~7行），浏览器验证 slotRows=7 ✓
- 根因2（周六日）：renderWeek 硬编码 for w=1..5 只渲染周一~周五 → 改为 1..7，网格 7 列（44px+repeat(7,1fr)），表头"周六/周日"带日期
- 根因3（假期周无法拖动）：格子内容此前用 lessonsOn(该周日期)（学期过滤，假期返回空→格子空→拖不了）→ 改为"基课表模板+临时调课标记"：任意周都显示每周固定14节课格，可拖动调课（补课/调休场景由此恢复）
- 细节：isOut（调出标记）改为仅当 该日期在 OVR 中有真实调整 时才显示，假期周不再误标整版"调出"
验证：周视图 7 节次行×7 列(周一~周日)、14 课格、liftCells=0 ✓

构建：我的课表-v1.9.apk（注：网络环境切换频繁，APK 内 www 已与最新原型同步）
